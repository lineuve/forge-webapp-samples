var CLIENT_ID = '<your client id>'; // Your app key
var REDIRECT_URI = '<address of app\'s home>/plugins/login/login-callback.html';// A redirect URI for your application
var ENV = '<forge environment>'; // dev / stg / prd
var SERVER_2_LEGGED = '<server endpoint that returns a 2-legged token>';
var SERVER_3_LEGGED = '<server endpoint that returns a 3-legged token>';
